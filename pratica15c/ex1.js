const estudante = ['João', 'Paulo', 'Mateus', 'Pedro', 'Tiago'];
const nota = [8, 5, 2, 10, 9];

for (let i = 0; i < estudante.length; i++) {
    console.log(`O estudante ${estudante[i]} tirou a nota ${nota[i]}`);
}
